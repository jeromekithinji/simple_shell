#include "main.h"

/**
  * add_numbers - Adds two numbers.
  *
  * @a: number to be added to.
  * @b: number to add to other number.
  * Return: sum of the numbers.
  */

int add_numbers(int a, int b)
{
	return (a + b);
}
