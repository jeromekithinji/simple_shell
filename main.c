#include <stdio.h>
#include "main.h"

/**
  * main - entry point.
  *
  * Return: 0 on success.
  */

int main(void)
{
	int result = add_numbers(5, 10);

	printf("The result is: %d\n", result);

	return (0);
}
