#ifndef MAIN_H
#define MAIN_H

#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
#include <string.h>

int add_numbers(int a, int b);
int execute_command(char *command);
ssize_t get_command(char *command);

#endif
